# RAID log — Fulmar 1.2.36 build 156

## Risks

| ID | Risk | Impact / likelihood | Mitigation and trigger | Status |
| --- | --- | --- | --- | --- |
| R1 | DSH `0.1.1-rc.1` changes startup, WebUI, RPC, provider, session, plugin, or MCP contracts. | High / Medium | Exact pin, generated contract tests, empty/cloned-state canaries, pre-upgrade backup, rollback. Requalify any pin change. | Mitigated; monitor |
| R2 | Model/provider switch leaves split DSH/native/consent state or leaks old context. | Critical / Low | One revision-aware transaction with rollback; exact-origin egress; runtime restart; fresh main/Quick Chat sessions; failure simulation. | Implemented; qualification pending |
| R3 | Cloud/provider request discloses more context than expected. | High / Medium | Broad disclosure warning, visible boundary, exact origin, fresh sessions, Skills/MCP policies, per-provider test account. | Residual user/provider risk |
| R4 | Cloud API behavior, billing, quotas, retention, or tools differ from simulated contracts. | High / Medium | Do not claim live validation; use non-production keys and low limits for explicit provider smoke tests. | Open until a funded disposable credential and quota are available |
| R5 | Configured custom endpoint is malicious, misclassified, or resolves unsafely. | High / Low | Valid HTTP(S) origin, HTTPS for cloud, strict hostname grammar, literal metadata/link-local/multicast rejection, unknown-as-cloud, new consent on edit. | Mitigated; DNS/TLS/provider residual |
| R6 | Workspace path mismatch causes tool or schedule failures. | High / Low | One controller-owned canonical Workspace injected into all paths; integration matrix asserts exact working directory. | Code issue closed; regression required |
| R7 | `sandbox-exec` changes or nested confinement fails on a macOS release. | High / Medium | Exact grammar probes, no fallback, independent preload/plugin controls, minimum/current OS tests. | Monitor each macOS update |
| R8 | Approved plugin or MCP child bypasses Node network guards. | Critical / Low | Interpose subprocess launch, supervisor-child sandbox, minimal env, private-store/egress denial, local stdio MCP only. | Mitigated; hostile-child test required |
| R8a | A hostile tool deliberately detaches from the runner process group and survives task cancellation. | Medium / Low | Parent-death group lease, bounded TERM/KILL, direct detach-syscall denial, inherited Seatbelt confinement, and explicit unsupported-daemon policy. | Residual: alternate spawn APIs are not a macOS job object; use VM/container isolation for hostile arbitrary executables |
| R9 | Skill prompt injection or reviewed resources leak to an external model. | High / Medium | Inert bounded import, fingerprint, per-project policy, local-only/ask/allow disclosure, read-only active catalog, fresh sessions. | Residual semantic risk |
| R10 | MCP executable, script, interpreter, or config changes after approval. | Critical / Medium | Fingerprint all reviewed components and project identity; automatic revocation; no shell/dynamic shebang; per-call native approval. | Mitigated; mutation matrix required |
| R11 | MCP output/tool count/startup/reconnect exhausts resources. | High / Medium | Enforced startup/call/tool/output/reconnect limits and cancellation; hostile server fixtures. | Implemented; qualification pending |
| R12 | Download is disguised executable/active content or changes after review. | High / Medium | Private no-follow staging, byte cap, extension/MIME/signature checks, quarantine, digest/metadata revalidation, explicit save. | Implemented; hostile corpus required |
| R13 | Workspace recovery deletes or overwrites newer work. | Critical / Low | State-bound preview, separate overwrite/remove options, symlink/type rejection, stored hash verification, rollback journal, injected-failure tests. | Implemented; qualification pending |
| R14 | Recovery omits a file the user expects. | Medium / Medium | Visible exclusions/limits and preview; manual source control recommended; fail closed on scan races/limits. | Accepted limitation |
| R15 | A backup, export, log, or support report misses an innocently named, novel, encoded, or unlabelled secret. | High / Medium | Keep values in Keychain; exclude classified paths; buffer service streams to complete lines; redact labelled/URL/Bearer credentials, oversized/multiline private keys, JWTs and reviewed provider-token families; re-sanitize support reports and private paths at copy; retain private permissions and sharing warnings. | Residual: pattern matching cannot prove arbitrary content is secret-free; inspect before sharing |
| R16 | 27B Deep profile causes memory pressure or slow multi-round work. | Medium / High | Balanced default, one loaded model/generation, exact-Qwen-only q8 KV/Flash Attention, bounded output, telemetry, cancellation/unload. Compatibility models use conservative settings without those model-specific optimizations. | Performance tuning ongoing |
| R17 | The separately installed Ollama executable is part of the trusted computing base. | Critical / Low | Require the official strict signature/team/identifier; bind canonical inode/CDHash to the exact PID/listener; use private HOME/TMP, read-only validated model store, cloud-off configuration, and loopback-only bind/egress sandbox. | Mitigated; correctly signed binary and arbitrary-loopback residual remain |
| R21 | The owned Ollama child exits after listener verification and a targeted same-UID process rebinds its random TCP port before a prompt connection. | High / Very low under trusted-account assumption | Random reserved port, exact PID/listener and dynamic-signature checks before exposure and around readiness, immediate child-exit handling that stops Harness. Do not describe raw HTTP as per-request PID authentication; an accepted-socket-verifying app proxy is required if same-user hostile processes enter scope. | Accepted single-user residual; compromised same-user processes are out of scope |
| R18 | Fulmar name/icon or upstream compatibility marks create confusion or third-party-rights risk. | High binary/marketing; Medium source preview | Owner authorizes the current name and generated icon for the source preview; keep independent branding and attribution; claim no affiliation, registration, originality, exclusivity or formal legal opinion. Require a separate formal decision before binary distribution or broader marketing. | Owner-accepted source-preview risk; binary/marketing gate remains |
| R19 | A private candidate is not notarized and cannot prove a public same-team update. | High public / Certain | The private-only external coordinator requires a matching persistent private certificate, exact candidate/stage/current/rollback proofs, stopped processes, an immutable durable pre-swap journal, atomic exchange, post-proof, durable receipt or explicit receipt finalization, and one retained rollback. Explicit crash-safe cancel/retire archives bundles and records without deletion. Developer ID, notarization, stapling, Gatekeeper and a two-version clean-Mac test remain mandatory for public binary distribution. | Private path implemented; public-binary blocker remains |
| R20 | Automated qualification gives false confidence. | High / Medium | Separate plan from evidence; require a stable signer, owner-only exact-candidate transcript, schema-2 record and full-hardware summary; validate supplied notary records strictly; keep installed/manual/external rows separate; require the exact eight-record candidate-bound external-evidence file before public success; record versions and hashes; make no zero-defect claim. | Public verifier now fails closed on absent/stale external records; build-156 and the real external evidence remain pending |
| R22 | A local conversation silently gains cloud web/image/voice/connector egress through a shared provider process. | Critical / Medium | General cloud capabilities remain disabled. Exact page fetch is a qualified exception with a per-call exact-URL Harness approval, an isolated short-lived preload capability, public-address/redirect/MIME/size controls, and no conversation-provider credential or grant reuse. | Page fetch implemented in 1.2.13; other routes remain disabled |
| R23 | An unavailable DeepSeek search tool causes a local model to fail, then retry via blocked shell networking. | High / High | Do not register `web_search` without an executable search route; expose only approved `web_fetch`; add fail-safe prompt guidance and a local-Qwen-plus-URL release scenario. | Fix implemented; live regression required |
| R24 | macOS can hide a valid third-party menu-bar item, and parallel Fulmar copies or programmatic removal with an attached autosave identity can corrupt placement evidence. | Medium / Observed | Use only public AppKit; retain one `Fulmar.MenuBar.v2` item while running; initialize that new identity visible exactly once; detach its autosave identity before programmatic removal; if AppKit says the item is visible while Fulmar's own button window is outside every top menu-bar band, perform at most two detached recreations without Accessibility access; never override `isVisible == false`; keep Dock/window/menu access authoritative; fail closed on any same-bundle peer; use complete BSD enumeration, kernel process identity, five-second AX identity-and-geometry dwell, real menu interaction, 20-cycle isolated and handoff soaks, and separate human pixel check. | Build 146 failed launch 8/20, build 147's fresh v2 identity failed launch 1/20, and build 148 passed 14 launches before failing launch 15/20 at the exact parking coordinate. Build 149 then passed 60 consecutive cold launches, but its synthetic handoff verifier intermittently retained a pre-recovery AX handle. Build 150 fixed that race, then failed cycle 1 because Launch Services returned before accessory-policy observation settled. Build 151 adds a bounded no-visible-UI accessory settlement proof and retains the fresh, identity-stable 20-cycle menu gate. Build 152 adds a release-only disposable-state physical scheduler-to-foreground proof while leaving ordinary launch environments unchanged. Build 153 corrected the normal-action verifier's model/boundary subtitle matching without weakening Chat or Settings checks. Build 154 adds comprehensive native-window geometry/action coverage. Build 155 extends Aqua/Dark minimum-layout and recovery-state coverage. Build 156 retains those controls and requires fresh exact-candidate installed menu/display evidence. |
| R25 | An update replaces the app but the new bundle fails before authenticated readiness, or power loss interrupts the two renames. | Critical public / Medium | Use the durable two-phase transaction: exact new-bundle launch, private nonce-bound PID/identity and Harness-health response before commit, automatic rollback or fail-closed surfaced recovery on every missing/invalid/interrupted boundary; add an approved separately signed recovery authority for the no-app-at-current-path interval; exercise across two notarized same-team versions. | **Public blocker:** journal/health/replay core and deterministic fault tests exist, but the between-renames reboot interval has only preserved manual rollback, and no two-version real-signed/notarized/stapled or physical-power-loss qualification exists; the menu remains disabled |
| R26 | Semgrep's mutable `p/default` endpoint reorders unchanged rules or changes rule content, making a raw-response pin either flaky or unsafe to repin. | High CI / Observed | Exact origin/type/size/count plus a length-framed byte-exact rule-block-set digest that ignores only top-level ordering; private old/new object comparison and full-scan equivalence before any content-pin change; scheduled monitoring after publication. | Ordering defect fixed before build 137; semantic endpoint drift remains fail-closed and monitored |
| R27 | Upstream DSH publishes a newer prerelease while Fulmar's exact pinned runtime remains under qualification. A hurried package-only update could reintroduce request identifiers, break MCP peers, or invalidate the signed runtime inventory. | Critical privacy/runtime / Observed | Run the non-destructive exact-version upgrade assessor, require every first-party DSH package to resolve to that exact version, verify reviewed registry provenance, inventory the complete first-party cohort, review source and dependency deltas, reapply or retire every vendored patch deliberately, then rebuild and rerun the complete migration/provider/tool/security/rollback matrix. Never hot-swap DSH in an installed app. | `0.1.1-rc.2` was assessed on 2026-08-29, `0.1.2-alpha.2` on 2026-08-31, and `0.1.2-alpha.3` on 2026-09-01; none was promoted. The corrected alpha.3 assessment verifies the root npm signature, binds and inventories all 215 first-party packages, resolves 580 production packages, and records 2,301 first-party file changes (1,606 code/config/manifest-sensitive), 68 root dependency changes, upstream stable identifier/session headers, and a mismatched guarded-MCP peer contract. Npm `alpha` and official GitHub release 381145530 now identify unpromoted `0.1.2-alpha.5` at commit `db6bdc3576c2d4e7c965e8e3ed0c2a731eed87f5`; its fix for rc.2/alpha.3 upgrade startup and session-title loss requires a separate exact-cohort assessment and cumulative retained-state migration qualification. `0.1.1-rc.1` remains the reviewed pin pending a full promotion. |
| R28 | Process loss between a Keychain mutation and metadata commit leaves a false configured state or loses the prior reference. | Critical / Low | Use one fixed owner-only global lock; fsync a bounded transaction journal before mutation; verify after mutation and metadata commit; atomically commit metadata; recover create/replace/adoption/remove/foreground repair; never overwrite an unexpected third value. | Source state-machine tests and the 60-case release-mode file-backed SIGKILL gate (24 ordinary, 18 version-two foreground repair, six token-bound malformed-record removal, twelve persistence-boundary cases) pass; exact-candidate real-Keychain canary remains pending |
| R31 | The credential helper is mistaken for isolation from a hostile process already running as the same macOS user. | Critical public / Low under the single-user assumption | Deny helper/private-store access from agent tools, keep unattended calls noninteractive, bind native calls to the signed bundled helper, and disclose the boundary. Replace stdout credential retrieval with authenticated IPC/XPC before expanding the threat model. | Accepted private single-user residual; public security-claim blocker |
| R32 | Credential transaction equality digests disclose a testable verifier for a low-entropy secret. | High / Low for provider API keys | Keep journals owner-only, bounded, short-lived, and free of literal values; require high-entropy provider credentials; consider a protected keyed commitment in a future journal version. | Residual disclosed; not literal credential storage |
| R29 | Constructed layout/action tests are mistaken for complete installed usability. | High public / Medium | Test minimum geometry in Aqua/Dark, 900×600 recovery states and action/accessibility metadata; separately run the exact installed local→cloud→local model-picker flow, VoiceOver/keyboard, permissions, displays and status-menu gates. | Automated source coverage expanded; installed AX/provider-switch and manual matrix deferred |
| R30 | English-only UI and the absence of a dedicated first-run assistant cause setup failure or are presented as localization/onboarding support. | Medium / Certain | Disclose English-only scope; keep Getting Started and typed recovery/settings paths; run clean-state usability acceptance; add localization/pseudolocalization and a guided setup journey before claiming either capability. | Accepted private limitation; public usability follow-up open |

## Assumptions

| ID | Assumption | Validation plan | Status |
| --- | --- | --- | --- |
| A1 | The deployment is a private, single-user Apple-silicon Mac with 48 GB unified memory. | Record hardware/OS in release evidence. | Expected; verify |
| A2 | Ollama is trusted and maintained separately; Fulmar launches its own tuned instance on a reserved random loopback port and never adopts a system-wide listener. | Executable ownership/mode checks plus exact child PID, listener, bounded health, and post-response ownership probes on each release. | Verify during qualification |
| A3 | `qwen3.8:27b-mlx` is installed and suitable for DSH tools. | Tags plus cold/warm completion and live tool matrix. | Verify during qualification |
| A4 | DSH provider settings expose credentials through the installed Keychain adapter. | CRUD/migration and UI-provider configuration test with non-secret fixtures. | Automated test required |
| A5 | The user wants no automatic cross-boundary context transfer. | Fresh-session acceptance criteria. | Accepted product decision |
| A6 | Heavy resources should remain on demand. | Login/scheduler defaults, model residency and quit tests. | Accepted product decision |
| A7 | Private use of current branding is acceptable to the owner. | Keep release private; obtain advice before distribution. | Accepted only for private use |

## Issues and decisions

| ID | Issue | Decision | Status |
| --- | --- | --- | --- |
| I1 | Fixed unauthenticated port 3080 could be adopted after restart. | Random authenticated port/token/identity on every launch; never adopt an existing service. | Closed in 1.0 |
| I2 | Missing `OLLAMA_API_KEY` made DSH reject a local route, while a process-wide marker could incorrectly make dormant Ollama look credential-ready in cloud or repair mode. | Supply one fixed non-secret readiness marker only to an inference runtime whose exact app-owned Ollama PID, signed identity, and random loopback listener have already been verified; omit it from cloud and provider-repair runtimes. | Closed in 1.1 |
| I3 | Whole-process Seatbelt caused forbidden nested DSH tool sandboxing. | Authenticate/confine DSH at Node boundary and inject one validated per-tool runner. | Closed in 1.0.2 |
| I4 | Native subprocesses could bypass Node egress guard. | Interpose non-tool child APIs and launch through supervisor-child confinement. | Closed in 1.0.3 |
| I5 | Hard links could expose an inode also named outside Workspace. | Scan aliases before tool launch; add explicit denies; fail closed on scan failure. | Closed in 1.0.3 |
| I6 | Native Quick Chat and filesystem sandbox used different Workspace paths. | Controller now supplies one authoritative Workspace to all consumers. | Closed in 1.1; live regression pending |
| I7 | A provider switch could retain old prompt/tool/Skill context. | Restart authenticated runtime and force fresh main/Quick Chat sessions after route/boundary changes. | Closed in 1.1; regression pending |
| I8 | Upstream MCP client lacked wrapper-level tool/output/startup limits and inherited too much environment. | Guarded local-stdio adapter, native per-call approval, limits, reduced child env; remote MCP disabled. | Closed in 1.1; qualification pending |
| I9 | Passive file extensions could cause mismatched downloads to be previewed. | Content signature/MIME mismatch warns and disables passive preview; revalidate before save. | Closed in 1.1; regression pending |
| I10 | Legacy credential migration requires real user consent. | Preserve source until verified Keychain read-back; leave final action to user. | Manual item open |
| I11 | The production dependency audit requires disclosing the pinned package graph to the configured registry. | Require explicit network authorization, record the exact credential-free HTTPS registry and lock digest, and fail release verification when current evidence is absent. | Blocked pending authorization; no pass inferred |

## Dependencies

| ID | Dependency | Contract | Status |
| --- | --- | --- | --- |
| D1 | DeepSeek Harness | `@deepseek-ai/dsh` 0.1.1-rc.1 | Bundled prerelease |
| D2 | DSH MCP client | Local stdio through guarded adapter; no remote transport | Bundled prerelease; pin/verify |
| D3 | Node.js | Apple-silicon 22.23.1 | Bundled/checksummed |
| D4 | Ollama | Installed binary and model store; app starts a fresh PID-verified loopback service | External dependency, app-owned process |
| D5 | Cloud providers | User account, terms, endpoint, key, quota, billing | Optional; funded disposable live-test credentials/quota unavailable |
| D6 | macOS | 15+; WebKit, Keychain, Vision, Speech, ScreenCaptureKit, ServiceManagement, Seatbelt behavior | Required; manual permission tests |
| D7 | Apple Developer ID/notary | Stable team identity and notarization service | Unavailable; public blocker |
| D8 | Branding/legal clearance | Fulmar name, original icon provenance, third-party attribution and release copy | Unresolved; public blocker |
